export interface IAddress {
    id: number
    street: string
    neighborhood: string
    city: string
    state: string
    zipCode: string
    customerId: number
    createdAt: Date
    updatedAt: Date
}
